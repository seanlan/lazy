{{if .NewFile}}
//generated by lazy
//author: seanlan

package service

import (
	"context"
	"{{.PackageName}}/{{.ModelPath}}"
)
{{end}}
func {{.ServiceName}}(ctx context.Context, userID int64, req model.{{.ReqStructName}}) (err error, resp model.{{.RespStructName}}) {
	return
}