{{if .NewFile}}
//generated by lazy
//author: seanlan

package v1

import (
	"github.com/gin-gonic/gin"
	"github.com/seanlan/goutils/xlerror"
    "github.com/seanlan/goutils/xlhttp"
	"{{.PackageName}}/{{.ModelPath}}"
	"{{.PackageName}}/{{.ServicePath}}"
)
{{end}}
func {{.ApiName}}(c *gin.Context) {
	var (
        err    error
        userID int64
    )
    r := xlhttp.Build(c)
    userID, err = r.GetJWTUID()
    if userID == 0 || err != nil {
        r.JsonReturn(xlerror.ErrToken)
        return
    }
	var req model.{{.ReqStructName}}
	err = r.RequestParser(&req)
    if err != nil {
        return
    }
    err, resp := service.{{.ServiceName}}(c, userID, req)
    r.JsonReturn(err, resp)
    return
}